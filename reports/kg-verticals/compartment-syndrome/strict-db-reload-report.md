# Compartment Syndrome Strict Database Reload

The legacy report's 35 aggregate-created structural objects were reconstructed exactly from the immutable approved packet: 14 entities, 20 relationships, and 1 curriculum bridge. The per-type counts and 35 unique fingerprints matched the aggregate apply counts before any membership write.

A membership-only repair reused all 35 canonical targets and inserted 35 batch memberships without changing canonical rows. The strict database audit scored 80 with no fallback. Publication remains `NOT_READY`; no publication gate was relaxed.
