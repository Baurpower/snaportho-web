import{NextResponse}from"next/server";import{deviceAuth,loadReleaseManifest}from"../../../_lib";export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){const a=await deviceAuth(request);if("response"in a)return a.response;const{id}=await params,manifest=await loadReleaseManifest(a.supabase,id);if(!manifest)return NextResponse.json({error:"release manifest unavailable"},{status:404});if(manifest.releaseStatus!=="published")return NextResponse.json({error:"release is not published"},{status:403});return NextResponse.json(manifest);}
