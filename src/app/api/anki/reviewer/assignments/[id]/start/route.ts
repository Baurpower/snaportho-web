import{NextResponse}from"next/server";import{reviewerAuth}from"../../../_lib";export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params,a=await reviewerAuth(request);if("response"in a)return a.response;const{data,error}=await a.auth.supabase.from("anki_review_assignments").update({status:"in_progress",started_at:new Date().toISOString()}).eq("id",id).eq("assigned_reviewer_id",a.ctx.userId).eq("status","assigned").select("id,status").maybeSingle();return error||!data?NextResponse.json({error:"assignment cannot start"},{status:409}):NextResponse.json(data);}
