import{NextResponse}from"next/server";import{reviewerAuth,audit}from"../../_lib";export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params,a=await reviewerAuth(request);if("response"in a)return a.response;const{data,error}=await a.auth.supabase.from("anki_review_assignments").select("id,status,assignment_type,deck_release_id,input_manifest_checksum,due_at,anki_review_assignment_items(*)").eq("id",id).eq("assigned_reviewer_id",a.ctx.userId).maybeSingle();if(error||!data)return NextResponse.json({error:"assignment not found"},{status:error?500:404});await audit(a.auth.supabase,a.ctx,"assignment_downloaded",{assignment_id:id});return NextResponse.json({assignment:data});}
