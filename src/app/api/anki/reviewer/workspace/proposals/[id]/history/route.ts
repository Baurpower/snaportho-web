import{NextResponse}from"next/server";import{reviewerAuth}from"../../../../_lib";
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){const a=await reviewerAuth(request);if("response"in a)return a.response;const{id}=await params,{data,error}=await a.auth.supabase.from("anki_editor_workspace_review_actions").select("id,reviewer_user_id,decision,proposal_evidence_hash,reviewer_roles_snapshot,reason_codes,reviewer_notes,client_version,created_at").eq("proposal_id",id).order("created_at");if(error)return NextResponse.json({error:"proposal history unavailable"},{status:500});return NextResponse.json({proposalId:id,actions:data??[]});}
